return table.freeze({
	LOADER_SLOT_COUNT = 30,
	LOADER_OUTPUT_INTERVAL = 1.5,
	RECYCLER_PROCESS_TIME = 15,
	PRESS_PROCESS_TIME = 20,
	RECYCLER_MATERIAL_RATIO = 5,
	PRESS_BLOCK_RATIO = 10,
	OUTPUT_CLEARANCE_SIZE = Vector3.new(4, 4, 4),
	CONVEYOR_SCAN_INTERVAL = 0.1,
	CONVEYOR_SPEED = 6,
	RECYCLER_CAPTURE_RADIUS = 6,
	INDUSTRIAL_PRESS_CAPTURE_RADIUS = 6,
	INTAKE_DEBUG_INTERVAL = 0.5,
	RECYCLER_GEAR_SPEED_DEGREES = 120,
	STATUS_UPDATE_INTERVAL = 0.2,
	CONVEYOR_TEXTURE_SPEED = 3,
	PICKUP_DISTANCE = 10,
	PERSISTENT_QUEUE_LIMIT = 1000,
	PERSISTENT_OUTPUT_LIMIT = 1000,
	SPAWN_RESPAWN_TIME = 30,
	FINAL_FACTORY_SLOTS = 2,
	NAVAL_SEAPLANE_BUILD_TIME = 60,
	NAVAL_SEAPLANE_RECIPE = table.freeze({MetalPanelStack = 40, PlasticPanelStack = 30}),
	-- Local to ItemOutput. The oriented bounding-box bottom is grounded on its top face.
	NAVAL_OUTPUT_POSITION_OFFSET = Vector3.new(0, 0, 0),
	NAVAL_OUTPUT_ROTATION_OFFSET_DEGREES = Vector3.new(0, 270, 0),
	NAVAL_OUTPUT_CLEARANCE = 0.03,
	-- Temporary Factory V1 development bootstrap. Remove this block after validation.
	ENABLE_TEST_BOOTSTRAP = false,
	TEST_BOOTSTRAP = table.freeze({
		AutomaticLoader = 1,
		Recycler = 1,
		IndustrialPress = 1,
		ConveyorStraight = 1,
		MetalWaste = 20,
	}),
})
