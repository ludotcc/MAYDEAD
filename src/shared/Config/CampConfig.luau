return {
	-- Test balancing requested for Camp V1. Change this single value for final balancing.
	CampFullRecoveryDuration = 30,
	UPDATE_INTERVAL = 0.25,
	MAX_INTERACTION_DISTANCE = 10,
	CampBedVerticalOffset = 0.5,
	EXIT_SIDE_CLEARANCE = 2.5,
	EXIT_VERTICAL_OFFSET = 2.5,
}
