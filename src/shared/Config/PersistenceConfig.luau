local RunService = game:GetService("RunService")

return table.freeze({
	SCHEMA_VERSION = 1,
	DATASTORE_NAMESPACE = RunService:IsStudio() and "DEV_V1" or "PROD_V1",
	AUTOSAVE_INTERVAL = 120,
	LOCK_LEASE_SECONDS = 120,
	LOCK_RENEW_INTERVAL = 30,
	MAX_RETRIES = 3,
	RETRY_DELAY = 1,
	CREATE_RESERVATION_TIMEOUT = 300,
	MAX_PERSISTENT_STRUCTURES = 10000,
	STRUCTURE_COUNT_WARNING = 8000,
	MAX_STRUCTURE_SNAPSHOT_BYTES = 3000000,
	STRUCTURE_SNAPSHOT_BYTES_WARNING = 2500000,
})
